groups:
  - name: linux
  targets:
    - pr_mozesnix0.classroom.puppet.com
  config:
    transport: ssh
    ssh:
      user: centos
      run-as: centos
      private-key: ~/.ssh/student.pem
      host-key-check: false
  - name: windows
  targets:
    - pr-mozeswin0.classroom.puppet.com
  config:
    transport: winrm
    winrm:
      user: administrator
      password: Puppetlabs!
      ssl: false
